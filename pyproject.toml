[project]
name = "dtcc-deploy"
version = "0.1.0"
description = "Deployment and tooling for the DTCC platform"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"
